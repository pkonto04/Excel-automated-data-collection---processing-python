# Excel-automated-data-collection---processing-python
A Python desktop application that automates the consolidation of Excel questionnaire submissions. The tool processes multiple client `.xlsx` files, extracts answers from a standardized template, validates response data, and generates a final Excel report with grouped responses, validation results, statistics, and charts.
